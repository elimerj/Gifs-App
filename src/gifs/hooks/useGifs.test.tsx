import { describe, test } from 'vitest';

describe('useGifs', () => {
  test('should return defaults values and methods', () => {});

  test('should return a list of gifs', () => {
    //handleSearch
  });
});
